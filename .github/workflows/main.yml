name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Set up Go 1.12
      uses: actions/setup-go@v1
      with:
        go-version: 1.12
      id: go
    
    - uses: actions/checkout@v1
    #- name: build Consul
      #run: make tools && make linux

    - name: Set GOPATH
      run: export GOPATH=$PWD
      
    - name: Check GOPATH
      run: ls $GOPATH
        
    #- name: check results
      #run: ./consul version
      
    #- name: Archive code coverage results
      #uses: actions/upload-artifact@v1
      #with:
        #name: consul-binary
        #path: consul
